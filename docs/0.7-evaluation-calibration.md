# 0.7 model evaluation and calibration (scikit-learn)

evaluation harness i can reuse across datasets: proper splits, no leakage, and calibration checks (not just accuracy).

## v1

- cv helpers: stratified, grouped, time series split
- metrics report:
  - roc-auc / pr-auc
  - log-loss, brier score
  - calibration curve (add ece if i implement it)
- calibration bake-off:
  - uncalibrated vs platt vs isotonic

## common mistakes to guard against

- leakage from preprocessing outside the cv loop
- calibrating on the test set
- treating metrics as stable when data is imbalanced

## upstream notes

- anything awkward about pipeline + calibratedclassifiercv
- docs that could be clearer about splits and calibration

## log

- yyyy-mm-dd:
